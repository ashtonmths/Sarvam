DATABASE_URL=postgres://ariadne:ariadne@localhost:5432/ariadne

# OpenRouter is the only external model provider. Embeddings run locally.
OPENROUTER_API_KEY=
OPENROUTER_MODEL_STRONG=
OPENROUTER_MODEL_BULK=

# Connectors (read-only scopes except where revert is enabled)
GITHUB_TOKEN=
SLACK_BOT_TOKEN=
AIRTABLE_TOKEN=
N8N_BASE_URL=
N8N_API_KEY=

# --- VPS deployment only (docker-compose.prod.yml) ---
DOMAIN=
POSTGRES_USER=ariadne
POSTGRES_PASSWORD=
N8N_ENCRYPTION_KEY=
